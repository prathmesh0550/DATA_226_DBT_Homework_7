name: 'build_wau'
version: '1.0.0'
config-version: 2
profile: 'build_wau'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  build_wau:
    +materialized: view
    input:
      +materialized: ephemeral
    output:
      +materialized: table